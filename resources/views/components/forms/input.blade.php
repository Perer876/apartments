<div class="col-md-4">
    <label for="{{ $name }}">{{ $label }}</label>
</div>
<div class="col-md-8 form-group">
    <input 
        class="form-control" 
        type="{{ $type }}" 
        id="{{ $name }}" 
        name="{{ $name }}" 
        placeholder="{{ $placeholder }}" 
    >
</div>